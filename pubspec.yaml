name: flutter_pokedex
description: A Pokedex made with Flutter for educational purpose.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  http: ^0.12.0+2
  flutter:
    sdk: flutter
  sqflite:
  path:
  shared_preferences:

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/pokemonDatabase.db
    - assets/pokemonSprites/